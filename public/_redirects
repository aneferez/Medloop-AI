# SPA fallback for Cloudflare Pages: serve index.html for client-side routes.
# Real static files (privacy.html, assets, etc.) are matched first and served
# directly; only unmatched paths fall through to the app.
/*    /index.html    200
